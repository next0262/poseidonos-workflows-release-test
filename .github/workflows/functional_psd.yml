
name: M2_Functional_PSD_Test

on:
  schedule:
    - cron: '0 10-21/4 * * MON-FRI'

  workflow_dispatch:
    inputs:
       logLevel:
          description: 'Log level'     
          required: true
          default: 'warning'
       tags:
          description: 'Test scenario tags'

jobs:
  check_date:
    runs-on: PSD
    name: Check latest commit
    outputs:
      should_run: ${{ steps.should_run.outputs.should_run }}
    steps:
    - run: |
        sudo chown -R $USER:$USER $GITHUB_WORKSPACE
    - uses: actions/checkout@v2
    - id: should_run
      name: check latest commit is less than 4 hours
      if: ${{ github.event_name == 'schedule' }}
      working-directory: ${{github.workspace}}
      run: 
        date=`date -d "-4 hours" "+%s"`; result=`git rev-list -1 --after=$date main`
        test -z $result && echo "::set-output name=should_run::false"

  Build:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute Build in M2_Functional_PSD_Test"

  Fault_Tolerance:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute Fault_Tolerance in M2_Functional_PSD_Test"

  File_Bench:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute File_Bench in M2_Functional_PSD_Test"

  IO_Path_System_Level:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute IO_Path_System_Level in M2_Functional_PSD_Test"

  Multi_Volume_IO:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute Multi_Volume_IO in M2_Functional_PSD_Test"


  NPOR:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute NPOR in M2_Functional_PSD_Test"


  NVMe_Flush:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute NVMe_Flush in M2_Functional_PSD_Test"

  QoS:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute QoS in M2_Functional_PSD_Test"


  SPOR:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute SPOR in M2_Functional_PSD_Test"

  Volume_Test:
    needs: check_date
    if: ${{ needs.check_date.outputs.should_run != 'false' }}
    runs-on: PSD
    steps:
    - run: |
        echo "execute Volume_Test in M2_Functional_PSD_Test"
